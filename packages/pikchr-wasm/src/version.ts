/**
 * The upstream pikchr check-in this package is built from.
 *
 * Kept in step with `vendor/pikchr.lock.json` by a test — the lock file is the
 * source of truth, this constant only makes it reachable from bundled code.
 */
export const PINNED_VERSION = "1.0 20260403102956";
